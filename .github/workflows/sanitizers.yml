---
name: "Sanitizers"

permissions:
  actions: write

on:
  # Run on pushes to main development branches
  push:
    branches: ["main"]
  # Run on all pull requests
  pull_request:
  # Allow manual triggering for debugging
  workflow_dispatch:

concurrency:
  group: ${{github.workflow}}-${{github.ref}}
  cancel-in-progress: true

env:
  CLANG_VER: 18
  LLVM_VER: 19.1.7    # https://github.com/llvm/llvm-project/releases
  HYPRE_VER: 2.19.0   # https://github.com/hypre-space/hypre/releases
  METIS_VER: 4.0.3
  # https://clang.llvm.org/docs/AddressSanitizer.html
  ASAN_OPTIONS: "detect_leaks=1,
                 strict_init_order=1,
                 strict_string_checks=1,
                 check_initialization_order=1,
                 detect_stack_use_after_return=1"
  # https://clang.llvm.org/docs/UndefinedBehaviorSanitizer.html
  UBSAN_OPTIONS: "halt_on_error=1, print_stacktrace=1"
  # https://clang.llvm.org/docs/MemorySanitizer.html
  MSAN_OPTIONS: "poison_in_dtor=1"

jobs:
  # man-split:
  #   runs-on: ubuntu-latest
  #   name: Manual split
  #   steps:
  #     - name: Touch
  #       run: man split

  # Build HYPRE
  build-hypre:
    runs-on: ubuntu-latest
    name: build-hypre
    steps:
      - name: Env
        run: |
          echo "HYPRE_DIR=hypre-${{env.HYPRE_VER}}" >> $GITHUB_ENV
          echo "HYPRE_TGZ=v${{env.HYPRE_VER}}.tar.gz" >> $GITHUB_ENV
      - name: Cache
        id: hypre-cache
        uses: actions/cache@v4
        with:
          path: ${{env.HYPRE_DIR}}
          key: ${{runner.os}}-ompi-build-${{env.HYPRE_DIR}}-int32-fp64-v2.5
      - name: Setup
        if: steps.hypre-cache.outputs.cache-hit != 'true'
        run: sudo apt-get install openmpi-bin libopenmpi-dev
      - name: Build
        if: steps.hypre-cache.outputs.cache-hit != 'true'
        uses: mfem/github-actions/build-hypre@v2.5
        with:
          archive: ${{env.HYPRE_TGZ}}
          dir: ${{env.HYPRE_DIR}}
          target: int32
          precision: fp64
          build-system: make

  # Build METIS
  build-metis:
    runs-on: ubuntu-latest
    name: build-metis
    steps:
      - name: Env
        run: |
          echo "METIS_DIR=metis-${{env.METIS_VER}}" >> $GITHUB_ENV
          echo "METIS_TGZ=metis-${{env.METIS_VER}}.tar.gz" >> $GITHUB_ENV
      - name: Cache
        id: metis-cache
        uses: actions/cache@v4
        with:
          path: ${{env.METIS_DIR}}
          key: ${{runner.os}}-build-${{env.METIS_DIR}}-v2.5
      - name: Setup
        if: steps.metis-cache.outputs.cache-hit != 'true'
        run: sudo apt-get install openmpi-bin libopenmpi-dev
      - name: Build
        if: steps.metis-cache.outputs.cache-hit != 'true'
        uses: mfem/github-actions/build-metis@v2.5
        with:
          archive: ${{env.METIS_TGZ}}
          dir: ${{env.METIS_DIR}}

  # Leak sanitizer suppression file
  build-file-lsan:
    runs-on: ubuntu-latest
    name: build-file-lsan
    steps:
      - name: Env
        run: |
          echo "ASAN_DIR=${GITHUB_WORKSPACE}/asan" >> $GITHUB_ENV
      - name: Setup
        run: |
          mkdir -p ${{env.ASAN_DIR}}
          cat << EOF > ${{env.ASAN_DIR}}/asan.supp
          leak:libevent_core-2.1.so
          leak:ompi_mpi_finalize
          leak:ompi_mpi_init
          leak:PMPI_Init
          leak:strdup # required Hypre 2.19.0
          EOF
      - name: Cache
        uses: actions/cache@v4
        with:
          path: cache
          key: cache-lsan-suppression-file

  # LLVM libcxx
  # https://github.com/llvm/llvm-project/tree/main/libcxx
  build-llvm-libcxx:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        sanitizer: [asan, msan, ubsan]
        include:
          - sanitizer: asan
            llvm_use_sanitizer: "Address"
          - sanitizer: msan
            llvm_use_sanitizer: "MemoryWithOrigins"
          - sanitizer: ubsan
            llvm_use_sanitizer: "Undefined"
    name: build-libcxx-${{matrix.sanitizer}}
    steps:
      - name: Env
        run: |
          echo "CC=clang-${{env.CLANG_VER}}" >> $GITHUB_ENV
          echo "CXX=clang++-${{env.CLANG_VER}}" >> $GITHUB_ENV
          echo "LLVM_DIR=${GITHUB_WORKSPACE}/llvm" >> $GITHUB_ENV
      - name: Cache
        id: llvm-cache
        uses: actions/cache@v4
        with:
          path: ${{env.LLVM_DIR}}
          key: Linux-llvm-${{env.LLVM_VER}}-${{matrix.sanitizer}}
      - name: Clone
        if: steps.llvm-cache.outputs.cache-hit != 'true'
        run: >
          git clone --filter=blob:none --depth=1
          --branch llvmorg-${{env.LLVM_VER}}
          --no-checkout https://github.com/llvm/llvm-project.git llvm-project
      - name: Checkout
        if: steps.llvm-cache.outputs.cache-hit != 'true'
        working-directory: llvm-project
        run: |
          git sparse-checkout set --cone
          git checkout llvmorg-${{env.LLVM_VER}}
          git sparse-checkout set cmake llvm/cmake runtimes libcxx libcxxabi
      - name: Mkdir
        if: steps.llvm-cache.outputs.cache-hit != 'true'
        run: mkdir ${{env.LLVM_DIR}}
      - name: CMake
        if: steps.llvm-cache.outputs.cache-hit != 'true'
        working-directory: ${{env.LLVM_DIR}}
        run: >
          VERBOSE=1
          cmake -GNinja ../llvm-project/runtimes/
          -DCMAKE_C_COMPILER=${{env.CC}}
          -DCMAKE_CXX_COMPILER=${{env.CXX}}
          -DCMAKE_BUILD_TYPE=RelWithDebInfo
          -DCMAKE_INSTALL_PREFIX=/usr
          -DLLVM_USE_SANITIZER=${{matrix.llvm_use_sanitizer}}
          -DLLVM_BUILD_32_BITS=OFF
          -DLIBCXXABI_USE_LLVM_UNWINDER=OFF
          -DLLVM_INCLUDE_TESTS=OFF
          -DLIBCXX_INCLUDE_TESTS=OFF
          -DLIBCXX_INCLUDE_BENCHMARKS=OFF
          -DLLVM_ENABLE_RUNTIMES='libcxx;libcxxabi'
      - name: Build
        if: steps.llvm-cache.outputs.cache-hit != 'true'
        working-directory: ${{env.LLVM_DIR}}
        run: cmake --build . -- cxx cxxabi

  # Build MFEM Serial with Sanitizers
  build-seq-mfem:
    needs: [build-file-lsan, build-llvm-libcxx]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        sanitizer: [asan, msan, ubsan]
        include:
          - sanitizer: asan
            cxxflags: "-fsanitize=address -fsanitize-address-use-after-scope"
            ldflags: "-fsanitize=address"
          - sanitizer: msan
            cxxflags: "-fsanitize=memory -fsanitize-memory-track-origins -fsanitize-memory-use-after-dtor"
            ldflags: "-fsanitize=memory"
          - sanitizer: ubsan
            cxxflags: "-fsanitize=undefined"
            ldflags: "-fsanitize=undefined"
    name: build-seq-mfem-${{matrix.sanitizer}}
    steps:
      - name: Cache
        id: mfem-cache
        uses: actions/cache@v4
        with:
          path: mfem-install
          key: cache-build-seq-mfem-${{matrix.sanitizer}}
      - name: Env
        run: |
          echo "ASAN_DIR=${GITHUB_WORKSPACE}/asan" >> $GITHUB_ENV
          echo "LLVM_DIR=${GITHUB_WORKSPACE}/llvm" >> $GITHUB_ENV
      - name: Restore LLVM libcxx
        if: steps.mfem-cache.outputs.cache-hit != 'true'
        uses: actions/cache@v4
        with:
          path: ${{env.LLVM_DIR}}
          key: Linux-llvm-${{env.LLVM_VER}}-${{matrix.sanitizer}}
      - name: Setup
        run: |
          echo "CC=clang-${{env.CLANG_VER}}" >> $GITHUB_ENV
          echo "CXX=clang++-${{env.CLANG_VER}}" >> $GITHUB_ENV
          echo "LLVM_INC=${LLVM_DIR}/include/c++/v1" >> $GITHUB_ENV
          echo "LLVM_LIB=${LLVM_DIR}/lib" >> $GITHUB_ENV
          echo LSAN_OPTIONS="suppressions=${{env.ASAN_DIR}}/asan.supp" >> $GITHUB_ENV
          echo "CTEST=ctest -j --test-load $(nproc) --stop-on-failure --schedule-random --output-on-failure --test-dir" >> $GITHUB_ENV
      - name: Setup (LLVM)
        run: |
          echo "LLVM_CXXFLAGS=-stdlib=libc++ -I${{env.LLVM_INC}} -Isystem${{env.LLVM_INC}}" >> $GITHUB_ENV
          echo "LLVM_LDFLAGS=-L${{env.LLVM_LIB}} -lc++abi -Wl,-rpath,${{env.LLVM_LIB}}" >> $GITHUB_ENV
      - name: Echo
        run: |
          echo "[CC] ${{env.CC}}"
          echo "[CXX] ${{env.CXX}}"
          echo "[LLVM_DIR] ${{env.LLVM_DIR}}"
          echo "[LLVM_INC] ${{env.LLVM_INC}}"
          echo "[LLVM_LIB] ${{env.LLVM_LIB}}"
          echo "[LLVM_CXXFLAGS] ${{env.LLVM_CXXFLAGS}}"
          echo "[LLVM_LDFLAGS] ${{env.LLVM_LDFLAGS}}"
          echo "[CTEST] ${{env.CTEST}}"
      - name: Checkout
        if: steps.mfem-cache.outputs.cache-hit != 'true'
        uses: actions/checkout@v4
        with:
          path: mfem
          repository: mfem/mfem  # 🔥
          ref: ubsan  # 🔥
      - name: Build
        if: steps.mfem-cache.outputs.cache-hit != 'true'
        uses: mfem/github-actions/build-mfem@v2.5
        with:
          mpi: seq
          mfem-dir: mfem
          hypre-dir: unused
          metis-dir: unused
          os: ${{runner.os}}
          library-only: true
          build-system: cmake
          config-options: >-
            -GNinja
            -DMPICXX=${{env.CXX}}
            -DCMAKE_CXX_STANDARD=17
            -DMFEM_USE_MEMALLOC=OFF
            -DCMAKE_BUILD_TYPE=Release
            -DCMAKE_VERBOSE_MAKEFILE=ON
            -DCMAKE_INSTALL_PREFIX=${GITHUB_WORKSPACE}/mfem-install
            -DCMAKE_CXX_COMPILER=${{env.CXX}}
            -DCMAKE_CXX_FLAGS_RELEASE='-g -O1 -fno-omit-frame-pointer ${{matrix.cxxflags}} ${{env.LLVM_CXXFLAGS}}'
            -DCMAKE_EXE_LINKER_FLAGS_RELEASE='${{matrix.ldflags}} ${{env.LLVM_LDFLAGS}}'
      - name: Install
        if: steps.mfem-cache.outputs.cache-hit != 'true'
        working-directory: mfem/build
        run: ninja install

  # Build MFEM Parallel with Sanitizers
  build-par-mfem:
    needs: [build-file-lsan, build-llvm-libcxx, build-hypre, build-metis]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        sanitizer: [asan, ubsan]
        include:
          - sanitizer: asan
            cxxflags: "-fsanitize=address -fsanitize-address-use-after-scope"
            ldflags: "-fsanitize=address"
          - sanitizer: ubsan
            cxxflags: "-fsanitize=undefined"
            ldflags: "-fsanitize=undefined"
    name: build-par-mfem-${{matrix.sanitizer}}
    steps:
      - name: Cache
        id: mfem-cache
        uses: actions/cache@v4
        with:
          path: mfem-install
          key: cache-build-par-mfem-${{matrix.sanitizer}}
      - name: Env
        run: |
          echo "ASAN_DIR=${GITHUB_WORKSPACE}/asan" >> $GITHUB_ENV
          echo "LLVM_DIR=${GITHUB_WORKSPACE}/llvm" >> $GITHUB_ENV
      - name: Restore LLVM libcxx
        if: steps.mfem-cache.outputs.cache-hit != 'true'
        uses: actions/cache@v4
        with:
          path: ${{env.LLVM_DIR}}
          key: Linux-llvm-${{env.LLVM_VER}}-${{matrix.sanitizer}}
      - name: Setup
        if: steps.mfem-cache.outputs.cache-hit != 'true'
        run: |
          sudo apt-get install openmpi-bin libopenmpi-dev
          echo CC=clang-${{env.CLANG_VER}} >> $GITHUB_ENV
          echo CXX=clang++-${{env.CLANG_VER}} >> $GITHUB_ENV
          echo LLVM_INC=${LLVM_DIR}/include/c++/v1 >> $GITHUB_ENV
          echo LLVM_LIB=${LLVM_DIR}/lib >> $GITHUB_ENV
          echo HYPRE_DIR=hypre-${{env.HYPRE_VER}} >> $GITHUB_ENV
          echo METIS_DIR=metis-${{env.METIS_VER}} >> $GITHUB_ENV
          echo LSAN_OPTIONS="suppressions=${{env.ASAN_DIR}}/asan.supp" >> $GITHUB_ENV
          echo "CTEST=ctest -j --test-load $(nproc) --stop-on-failure --schedule-random --output-on-failure --test-dir" >> $GITHUB_ENV
          echo "PRTE_MCA_rmaps_default_mapping_policy=:oversubscribe" >> $GITHUB_ENV
          echo "MPI_INC=$(mpicxx --showme:compile)" >> $GITHUB_ENV
          echo "MPI_LIB=$(mpicxx --showme:link)" >> $GITHUB_ENV
      - name: Setup (LLVM)
        if: steps.mfem-cache.outputs.cache-hit != 'true'
        run: |
          echo "LLVM_CXXFLAGS=-stdlib=libc++ -I${{env.LLVM_INC}} -Isystem${{env.LLVM_INC}}" >> $GITHUB_ENV
          echo "LLVM_LDFLAGS=-L${{env.LLVM_LIB}} -lc++abi -Wl,-rpath,${{env.LLVM_LIB}}" >> $GITHUB_ENV
      - name: Echo
        if: steps.mfem-cache.outputs.cache-hit != 'true'
        run: |
          echo "[CC] ${{env.CC}}"
          echo "[CXX] ${{env.CXX}}"
          echo "[LLVM_DIR] ${{env.LLVM_DIR}}"
          echo "[LLVM_INC] ${{env.LLVM_INC}}"
          echo "[LLVM_LIB] ${{env.LLVM_LIB}}"
          echo "[LLVM_CXXFLAGS] ${{env.LLVM_CXXFLAGS}}"
          echo "[LLVM_LDFLAGS] ${{env.LLVM_LDFLAGS}}"
          echo "[HYPRE_DIR] ${{env.HYPRE_DIR}}"
          echo "[METIS_DIR] ${{env.METIS_DIR}}"
          echo "[MPI] Version: $(mpicxx --showme:version)"
          echo "[MPI] Compile: $(mpicxx --showme:compile)"
          echo "[MPI] Link: $(mpicxx --showme:link)"
          echo "[CTEST] ${{env.CTEST}}"
      - name: Restore Hypre
        if: steps.mfem-cache.outputs.cache-hit != 'true'
        uses: actions/cache@v4
        with:
          path: ${{env.HYPRE_DIR}}
          key: ${{runner.os}}-ompi-build-${{env.HYPRE_DIR}}-int32-fp64-v2.5
      - name: Restore Metis
        if: steps.mfem-cache.outputs.cache-hit != 'true'
        uses: actions/cache@v4
        with:
          path: ${{env.METIS_DIR}}
          key: ${{runner.os}}-build-${{env.METIS_DIR}}-v2.5
      - name: Checkout
        if: steps.mfem-cache.outputs.cache-hit != 'true'
        uses: actions/checkout@v4
        with:
          path: mfem
          ref: ubsan
          repository: mfem/mfem  # 🔥🔥🔥 Using MFEM/ubsan repository
      - name: Build
        if: steps.mfem-cache.outputs.cache-hit != 'true'
        uses: mfem/github-actions/build-mfem@v2.5
        with:
          mpi: par
          mfem-dir: mfem
          os: ${{runner.os}}
          library-only: true
          build-system: cmake
          hypre-dir: ${{env.HYPRE_DIR}}
          metis-dir: ${{env.METIS_DIR}}
          config-options: >-
            -GNinja
            -DMPICXX=${{env.CXX}}
            -DCMAKE_CXX_STANDARD=17
            -DMFEM_USE_MEMALLOC=OFF
            -DCMAKE_BUILD_TYPE=Release
            -DCMAKE_VERBOSE_MAKEFILE=ON
            -DCMAKE_CXX_COMPILER=${{env.CXX}}
            -DCMAKE_INSTALL_PREFIX=${GITHUB_WORKSPACE}/mfem-install
            -DCMAKE_CXX_FLAGS_RELEASE='-g -O1 -fno-omit-frame-pointer ${{matrix.cxxflags}} ${{env.LLVM_CXXFLAGS}} ${{env.MPI_INC}}'
            -DCMAKE_EXE_LINKER_FLAGS_RELEASE='${{matrix.ldflags}} ${{env.LLVM_LDFLAGS}} ${{env.MPI_LIB}}'
      - name: Install
        if: steps.mfem-cache.outputs.cache-hit != 'true'
        working-directory: mfem/build
        run: ninja install

  # Check MFEM Serial with Sanitizers
  test-seq-check:
    needs: [build-seq-mfem]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        sanitizer: [asan, msan, ubsan]
    name: test-seq-check-${{matrix.sanitizer}}
    steps:
      - name: Setup
        run: |
          echo CC=clang-${{env.CLANG_VER}} >> $GITHUB_ENV
          echo CXX=clang++-${{env.CLANG_VER}} >> $GITHUB_ENV
          echo LLVM_DIR=${GITHUB_WORKSPACE}/llvm >> $GITHUB_ENV
      - name: Setup (LLVM_INC, LLVM_LIB)
        run: |
          echo LLVM_INC=${LLVM_DIR}/include/c++/v1 >> $GITHUB_ENV
          echo LLVM_LIB=${LLVM_DIR}/lib >> $GITHUB_ENV
      - name: Setup (LLVM_CXXFLAGS, LLVM_LDFLAGS)
        run: |
          echo "LLVM_CXXFLAGS=-stdlib=libc++ -I${{env.LLVM_INC}} -Isystem${{env.LLVM_INC}}" >> $GITHUB_ENV
          echo "LLVM_LDFLAGS=-L${{env.LLVM_LIB}} -lc++abi -Wl,-rpath,${{env.LLVM_LIB}}" >> $GITHUB_ENV
      - name: Restore
        id: mfem-cache
        uses: actions/cache@v4
        with:
          path: mfem-install
          fail-on-cache-miss: true
          key: cache-build-seq-mfem-${{matrix.sanitizer}}
      - name: Patch config.mk
        run: |
          sed -i -e '/^MFEM_LIBS/ s/$/ $(LLVM_LDFLAGS)/' mfem-install/share/mfem/config.mk
      - name: Restore LLVM libcxx
        uses: actions/cache@v4
        with:
          path: ${{env.LLVM_DIR}}
          fail-on-cache-miss: true
          key: Linux-llvm-${{env.LLVM_VER}}-${{matrix.sanitizer}}
      - name: Checkout
        uses: actions/checkout@v4
        with:
          path: mfem
          ref: ubsan
          repository: mfem/mfem  # 🔥🔥🔥 Using MFEM/ubsan repository
      - name: MFEM Check
        working-directory: mfem/examples
        # env:
        #   CXXFLAGS: ${{env.LLVM_CXXFLAGS}} ${{env.MPI_INC}}
        #   LDFLAGS: ${{env.LLVM_LDFLAGS}} ${{env.MPI_LIB}}
        run: MFEM_INSTALL_DIR=${GITHUB_WORKSPACE}/mfem-install make ex1-test-seq

  test-par-check:
    needs: [build-par-mfem]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        sanitizer: [asan, ubsan]
    name: test-par-check-${{matrix.sanitizer}}
    steps:
      - name: Setup
        run: |
          sudo apt-get install openmpi-bin libopenmpi-dev
          echo CC=clang-${{env.CLANG_VER}} >> $GITHUB_ENV
          echo CXX=clang++-${{env.CLANG_VER}} >> $GITHUB_ENV
          echo LLVM_DIR=${GITHUB_WORKSPACE}/llvm >> $GITHUB_ENV
          echo HYPRE_DIR=hypre-${{env.HYPRE_VER}} >> $GITHUB_ENV
          echo METIS_DIR=metis-${{env.METIS_VER}} >> $GITHUB_ENV
          echo "MPI_INC=$(mpicxx --showme:compile)" >> $GITHUB_ENV
          echo "MPI_LIB=$(mpicxx --showme:link)" >> $GITHUB_ENV
      - name: Echo
        run: |
          echo "[CC] ${{env.CC}}"
          echo "[CXX] ${{env.CXX}}"
          echo "[LLVM_DIR] ${{env.LLVM_DIR}}"
          echo "[HYPRE_DIR] ${{env.HYPRE_DIR}}"
          echo "[METIS_DIR] ${{env.METIS_DIR}}"
          echo "[MPI] MPI_INC: ${{env.MPI_INC}}"
          echo "[MPI] MPI_LIB: ${{env.MPI_LIB}}"
      - name: Setup (LLVM_INC, LLVM_LIB)
        run: |
          echo LLVM_INC=${LLVM_DIR}/include/c++/v1 >> $GITHUB_ENV
          echo LLVM_LIB=${LLVM_DIR}/lib >> $GITHUB_ENV
      - name: Setup (LLVM_CXXFLAGS, LLVM_LDFLAGS)
        run: |
          echo "LLVM_CXXFLAGS=-stdlib=libc++ -I${{env.LLVM_INC}} -Isystem${{env.LLVM_INC}}" >> $GITHUB_ENV
          echo "LLVM_LDFLAGS=-L${{env.LLVM_LIB}} -lc++abi -Wl,-rpath,${{env.LLVM_LIB}}" >> $GITHUB_ENV
      - name: Restore
        id: mfem-cache
        uses: actions/cache@v4
        with:
          path: mfem-install
          fail-on-cache-miss: true
          key: cache-build-par-mfem-${{matrix.sanitizer}}
      - name: Patch config.mk
        run: |
          sed -i -e '/^MFEM_CPPFLAGS/ s/$/ ${{env.MPI_INC}}/' mfem-install/share/mfem/config.mk
          sed -i -e '/^MFEM_LIBS/ s/$/ ${{env.LLVM_LDFLAGS}} ${{env.MPI_LIB}}/' mfem-install/share/mfem/config.mk
      - name: Cat config.mk
        run: cat mfem-install/share/mfem/config.mk
      - name: Restore LLVM libcxx
        uses: actions/cache@v4
        with:
          path: ${{env.LLVM_DIR}}
          fail-on-cache-miss: true
          key: Linux-llvm-${{env.LLVM_VER}}-${{matrix.sanitizer}}
      - name: Restore Hypre
        uses: actions/cache@v4
        with:
          path: ${{env.HYPRE_DIR}}
          fail-on-cache-miss: true
          key: ${{runner.os}}-ompi-build-${{env.HYPRE_DIR}}-int32-fp64-v2.5
      - name: Restore Metis
        uses: actions/cache@v4
        with:
          path: ${{env.METIS_DIR}}
          fail-on-cache-miss: true
          key: ${{runner.os}}-build-${{env.METIS_DIR}}-v2.5
      - name: Restore Hypre/Metis links
        run: |
            ln -s -f ${{env.HYPRE_DIR}} hypre
            ln -s -f ${{env.METIS_DIR}} metis-4.0
      - name: Checkout
        uses: actions/checkout@v4
        with:
          path: mfem
          ref: ubsan
          repository: mfem/mfem  # 🔥🔥🔥 Using MFEM/ubsan repository
      - name: MFEM Check
        # working-directory: mfem/examples
        # env:
        #   CXXFLAGS: ${{env.LLVM_CXXFLAGS}} ${{env.MPI_INC}}
        #   LDFLAGS: ${{env.LLVM_LDFLAGS}} ${{env.MPI_LIB}}
        run: MFEM_INSTALL_DIR=${GITHUB_WORKSPACE}/mfem-install make ex1p-test-par

  # test-seq:
  #   needs: [build-seq-mfem]
  #   runs-on: ubuntu-latest
  #   strategy:
  #     matrix:
  #       sanitizer: [asan, msan, ubsan]
  #       test: [examples, miniapps, tests]
  #   name: test-seq-${{matrix.sanitizer}}-${{matrix.test}}
  #   steps:
  #     - name: Restore
  #       uses: actions/cache@v4
  #       with:
  #         path: cache
  #         key:  cache-build-seq-${{matrix.sanitizer}}-${{github.sha}}
  #     - name: Run ${{ matrix.test }}
  #       run: |
  #         echo Running ${{ matrix.test }}

  # test-par:
  #   needs: [build-par-mfem, build-par-hypre, build-par-metis]
  #   runs-on: ubuntu-latest
  #   strategy:
  #     matrix:
  #       sanitizer: [asan, msan, ubsan]
  #       test: [examples, miniapps, tests]
  #   name: test-par-${{matrix.sanitizer}}-${{matrix.test}}
  #   steps:
  #     - name: Restore
  #       uses: actions/cache@v4
  #       with:
  #         path: cache
  #         key:  cache-build-par-${{matrix.sanitizer}}-${{github.sha}}
  #     - name: Run ${{ matrix.test }}
  #       run: |
  #         echo Running ${{ matrix.test }}

# ./unit_tests --filenames-as-tags --list-tags|grep '\[#' | cut -d '[' -f 2 | cut -d']' -f1 > tests
# ./unit_tests --filenames-as-tags --input-file ./tests
